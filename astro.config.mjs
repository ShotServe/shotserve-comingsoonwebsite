import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://rrocketter.github.io',
  base: 'https://github.com/ShotServe/shotserve-comingsoonwebsite',
})
